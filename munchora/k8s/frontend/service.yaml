apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  labels:
    app: frontend
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: munchora
spec:
  type: NodePort
  ports:
    - port: 30000
      targetPort: 30000
      nodePort: 30000
      protocol: TCP
  selector:
    app: frontend
