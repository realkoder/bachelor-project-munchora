apiVersion: v1
kind: Service
metadata:
  name: frontend-app
  labels:
    app: frontend-app
    app.kubernetes.io/name: frontend-app
    app.kubernetes.io/part-of: munchora
spec:
  type: ClusterIP
  ports:
    - port: 5173
      targetPort: 5173
      protocol: TCP
  selector:
    app: frontend-app
