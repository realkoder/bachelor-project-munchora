apiVersion: v1
kind: Service
metadata:
  name: recipes-mysql-service
  labels:
    app: recipes-mysql
    app.kubernetes.io/name: recipes-mysql
    app.kubernetes.io/part-of: munchora
spec:
  type: ClusterIP
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
  selector:
    app: recipes-mysql
