apiVersion: v1
kind: Service
metadata:
  name: ai-mysql-service
  labels:
    app: ai-mysql
    app.kubernetes.io/name: ai-mysql
    app.kubernetes.io/part-of: munchora
spec:
  type: ClusterIP
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
  selector:
    app: ai-mysql
