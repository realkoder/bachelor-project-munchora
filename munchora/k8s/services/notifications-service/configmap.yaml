apiVersion: v1
kind: ConfigMap
metadata:
  name: notifications-service-config
  labels:
    app.kubernetes.io/name: notifications-service
    app.kubernetes.io/part-of: munchora
data:
  RAILS_RELATIVE_URL_ROOT: '/notifications'
